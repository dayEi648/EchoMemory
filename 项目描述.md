# 项目描述

echomemory 是面向 Windows 桌面端的 AI 音乐平台，采用 Tauri 2 + React + FastAPI 架构，围绕音乐内容管理、AI 对话、RAG 知识库、社区审核与 Agent 可观测性建设完整业务闭环。

简历亮点：

- 多轮 Agent 对话容易丢上下文、工具调用难恢复：基于 LangGraph 编排对话节点与工具节点，引入 Postgres Checkpointer 持久化会话状态，支持流式响应和可恢复的多轮交互。
- AI 回答缺少知识依据：实现文档解析、文本切分、DashScope embedding、pgvector 向量检索链路，把音乐知识沉淀为可检索资料，降低纯模型回答的幻觉风险。
- Agent 运行过程难排查：设计异步旁路监控，采集运行、节点、LLM、工具调用等事件；监控写入失败只记录日志，不阻断用户侧对话主流程。
- 社区内容审核依赖人工、响应慢：实现 AI 内容审核 Worker，支持任务租约、失败重试、人工复核与恢复流程，提高评论、动态等内容治理的自动化程度。
- 推荐功能缺少可解释与可干预能力：实现“私人漫游”，用 Redis 维护临时偏好池、屏蔽池和播放位置；用户收藏/不喜欢会实时影响后续推荐，AI 生成推荐理由、解析自然语言引导并产出漫游品味总结。
